# Emergency Ambulance Dispatch and Tracking System

Java 17 Maven project with priority-based emergency dispatch, ambulance state tracking, waiting queue, custom exceptions, and positive/negative JUnit tests.

## Build

```bash
mvn clean test
mvn package
```
